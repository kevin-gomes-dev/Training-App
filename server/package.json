{
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.12.0",
    "pg": "^8.3.0"
  },
  "name": "training-app",
  "version": "1.0.0",
  "description": "TODO: Description, instructions for how to run server and client, DB schematics, how to use (typical workflow). TODO: Create boilerplate of frontend and backend, ideally use routes for both. Context manager over prop drilling? Note: Let's try to avoid direct pushes to main, and instead pull requests",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch --env-file=.env ./index.js",
    "db:schema": "psql -d training -f ./db/schema.sql",
    "db:seed": "node --env-file=.env ./db/seed.js",
    "db:reset": "npm run db:schema && npm run db:seed"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kevin-gomes-dev/Training-App.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bugs": {
    "url": "https://github.com/kevin-gomes-dev/Training-App/issues"
  },
  "homepage": "https://github.com/kevin-gomes-dev/Training-App#readme",
  "devDependencies": {
    "psql": "^0.0.1"
  }
}
